{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content -%}
    <h1>Composition</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('composition') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
