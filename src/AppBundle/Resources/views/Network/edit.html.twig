{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content -%}
    <div class="container">
        <h1>Network edit</h1>

        {{ form(edit_form) }}


        <a class="btn btn-primary" href="{{ path('network') }}">
            Zurück zur Übersicht
        </a>
        {{ form(delete_form) }}
    </div>
</ul>
{% endblock %}
