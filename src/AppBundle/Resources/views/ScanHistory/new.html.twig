{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ScanHistory creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('scan_history') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
