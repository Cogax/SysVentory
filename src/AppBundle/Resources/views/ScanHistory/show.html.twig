{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content -%}
    <h1>ScanHistory</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ entity.time|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Netrange</th>
                <td>{{ entity.netRange }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('scanhistory') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('scanhistory_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
